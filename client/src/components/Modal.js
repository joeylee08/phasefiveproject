const Modal = ({handleIsModal}) => {
  return (
    <>
      <div className="modal">
        <h1>Penis.</h1>
        <button onClick={handleIsModal}>CLOSE</button>
      </div>
    </>
  )
}

export default Modal